---
- name: Install Nginx on AWS EC2 Hosts
  hosts: aws_ec2
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install nginx
      apt:
        name: nginx
        state: present
